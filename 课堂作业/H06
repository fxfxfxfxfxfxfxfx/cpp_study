#include<iostream>
#include<iomanip>
#include<cmath>
using namespace std;

int main()
{
	int n, t;
	t = 1;
	cin >> n;
	for (int i = 3; i <= n; i++)
	{
		if (i % 3 == 0 || i % 5 == 0 || i % 7 == 0)
		{
			cout << setw(8) << i;
			t++;
		}
		if (t % 8 == 0)
		{
			cout << endl;
		}
	}
	return 0;
}
