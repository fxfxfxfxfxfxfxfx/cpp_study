#include <iostream>
#include <iomanip>
#include <cmath>
using namespace std;

int main()
{
	int n, sum;
	sum = 0;
	cin >> n;
	int a = 1;
	int b = 1;
	for (int i = 0; i < n; i++)
	{
		sum = sum + b;
		if (a == b)
		{
			a++;
			b = 1;
		}
		b++;
	}
	cout << sum;
	return 0;
}

